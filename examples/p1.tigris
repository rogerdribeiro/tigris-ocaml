{# programa para cálculo do fatorial de um
número inteiro #}

# comentário de uma linha
let
        var n: int := 7
        var f: int := 1
in
        while n > 0 do
                ( f := f * n;
                n := n - 1;
                );
        printint(f)
end