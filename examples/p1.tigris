let
        var n: int := 71
        var f: int := 1,1
in
        while n > 0 do
                if then else
                ( f := f * n;
                n := n - 1;
                );
        printint(f)
end